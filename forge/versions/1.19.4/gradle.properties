# Architectury Loom reads its platform from a Gradle property, which has to be
# in place before the plugin is applied - so it lives here rather than in the
# build script. Everything else about this target is in ../../build.gradle.
loom.platform = forge
